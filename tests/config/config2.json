{
  "plugins": [
    {
      "id": "plugin-1",
      "config": {
        "example": "test"
      }
    },
    {
      "id": "plugin-2",
      "config": {
        "differentExample": "another value"
      }
    }
  ]
}